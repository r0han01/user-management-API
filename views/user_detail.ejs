<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background-color: #f7f9fc; color: #333; padding: 20px; }
        h1 { text-align: center; margin-bottom: 20px; color: #4CAF50; font-size: 2em; letter-spacing: 1px; }
        .user-info { width: 50%; margin: 0 auto; background-color: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); }
        p { text-align: center; color: #888; font-size: 16px; }
        .details { margin-top: 20px; }
        .details dt { font-weight: bold; margin: 10px 0 5px; }
        .details dd { margin-bottom: 10px; }
    </style>
</head>
<body>
    <h1>User Details</h1>

    <div class="user-info">
        <h2><%= user.username %></h2>
        <div class="details">
            <dl>
                <dt>Password (hashed):</dt>
                <dd><%= user.password %></dd>
            </dl>
        </div>
        <p><a href="/users">Back to All Users</a></p>
    </div>
</body>
</html>
